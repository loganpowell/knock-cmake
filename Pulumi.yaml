name: knock-lambda
description: Knock ACSM converter Lambda infrastructure (base and environment stacks)
runtime:
  name: python
  options:
    toolchain: uv
main: infrastructure/
stackConfigDir: infrastructure/
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
