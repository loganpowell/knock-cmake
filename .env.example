# Example Environment Configuration
# Copy this file to .env and update with your values

# Pulumi Configuration
# Set this to your Pulumi config passphrase to avoid interactive prompts
PULUMI_CONFIG_PASSPHRASE=your_passphrase_here

# AWS Configuration (optional - can also use AWS CLI configured profile)
# AWS_PROFILE=your_profile
# AWS_REGION=us-east-2

# Docker Hub Credentials for ECR Pull-Through Cache
# For local development, set these in your .env file
# For CI/CD, these come from GitHub Secrets (DOCKER_HUB_USERNAME, DOCKER_HUB_TOKEN)
DOCKER_HUB_USERNAME=your_dockerhub_username
DOCKER_HUB_TOKEN=your_dockerhub_token_or_pat

# Test Configuration (optional)
# TEST_TIMEOUT=30
# TEST_RETRIES=3