# Example Environment Configuration
# Copy this file to .env and update with your values

# Pulumi Configuration
# Set this to your Pulumi config passphrase to avoid interactive prompts
PULUMI_CONFIG_PASSPHRASE=your_passphrase_here

# AWS Configuration (optional - can also use AWS CLI configured profile)
# AWS_PROFILE=your_profile
# AWS_REGION=us-east-2

# Docker Hub Credentials for ECR Pull-Through Cache
# These are now set via Pulumi config for local development:
#   pulumi config set dockerHubUsername your_username
#   pulumi config set --secret dockerHubToken your_token
# And via GitHub Secrets for CI/CD (DOCKER_HUB_USERNAME, DOCKER_HUB_TOKEN)
# No need to add them to this .env file

# Test Configuration (optional)
# TEST_TIMEOUT=30
# TEST_RETRIES=3